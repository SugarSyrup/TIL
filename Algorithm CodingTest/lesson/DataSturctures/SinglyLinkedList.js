class Node{
    constructor(value){
        this.value;
        this.next;
    }
}

class SinglyLinkedList{
    constructor() {
        this.head
        this.tail
    }
    find(value)
    apeend(newValue)
    insert(node, newValue)
    remove(value)
    display()
}